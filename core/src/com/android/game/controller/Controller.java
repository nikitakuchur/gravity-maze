package com.android.game.controller;

public interface Controller {
    /**
     * Update is called every frame
     *
     * @param deltaTime the completion time since the last frame
     */
    void update(float deltaTime);
}
